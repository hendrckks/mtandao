import { ElementType } from 'react';

export interface TooltipLinkProps {
  to: string;
  icon: ElementType;
  tooltip: string;
}
export interface NavitemProps {
  label: string;
  icon: ElementType;
  href: string;
}
